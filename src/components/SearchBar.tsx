export default function SearchBar() {
  return (
    <section>
      <input
        type="text"
        placeholder="Digite marca, modelo ou versão..."
      />

      <button>
        Buscar
      </button>
    </section>
  );
}